# CHANGELOG — MovieVault

> **MANDATORY FOR ALL AI AGENTS**
> After every session where code was changed, a decision was made, or a plan was modified — add an entry below under `## [Unreleased]`. Do not skip this even for small changes. Preserve all past entries.
>
> **Entry format:**
> ```
> ### YYYY-MM-DD — [short summary of session goal]
> - **Changed:** what files/components were touched and why
> - **Decided:** any architectural or design decisions made and the reasoning
> - **Deviations:** anything that differed from the plan, and why
> - **Known issues / next steps:** what was left open
> ```

---

## [Unreleased]

### 2026-07-06 — Archivist's Vault structural motifs (Phase 8 / Design Spells)
- **Changed:** `src/styles/global.css` — four film/reel structural elements added:
  (1) `.section-title::after` replaced from a plain 1px border line to a repeating film-strip tile (12px tall, dark amber `#100e08` background, punched sprocket holes via `radial-gradient`, `opacity: 0.55`) — every section header now trails off into a miniature film strip;
  (2) `.card::before` + `.card:hover::before` — 8-gradient CSS corner-bracket technique (4×2 `linear-gradient` stops, no SVG, no DOM nodes) draws amber film-frame registration marks at all four card corners, fading in on hover;
  (3) `.sidebar::after` — subtle 14px wide vertical sprocket strip along the sidebar's right edge using `background-image: radial-gradient` in repeat-y, amber-tinted, very low opacity (0.04 fill / 0.18 dots);
  (4) `.card--featured::after` — amber film-leader strip (10px tall, repeating radial-gradient dots) running across the top edge of every featured/wide Bento card;
  (5) Added `.card::before { transition: none !important; }` inside the existing `@media (prefers-reduced-motion: reduce)` block.
- **Decided:** "Archivist's Vault" aesthetic — a private film-collector's vault — chosen as the structural language after DFII 14 analysis via `/antigravity-awesome-skills:frontend-design`. Motif applied first to section dividers + card borders because they are the highest-frequency surfaces (user sees them on every page, every scroll) vs. sidebar texture (seen once) or loading states (seen only on cold start). All four implementations are pure CSS: zero new DOM nodes, no JS, no new dependencies. The corner-bracket technique uses only `background` multi-stop on a single pseudo-element — extremely low cost.
- **Deviations:** None planned. This was exploratory Phase 8 work not in the original V3 plan.
- **Known issues / next steps:** Not visually verified by the agent. User should run `npm run start` and hover cards on the Home page to verify the corner bracket effect. Featured card film strip is always-on (not hover-only) — intentional to differentiate featured tiles at a glance. Tier 2 Design Spells (#5–13 from the skills output) remain unstarted.

### 2026-06-18 — UI Redesign V3 Phase 7d: episode-page UX pass (closes Phase 7)
- **Changed:** `src/styles/global.css` — `.episode-card` now uses `var(--card-radius)`/`var(--card-shadow)`/`var(--card-shadow-hover)` instead of the old `var(--radius)`/no-shadow; `.episode-thumb`'s radius bumped from a hardcoded `5px` to `var(--radius)`; `.season-selector` converted from `flex-wrap: wrap` to a horizontal scroll row (`overflow-x: auto`, thin scrollbar), matching the pattern already used for OnePace's saga filters and `.scroll-row`.
- **Decided:** Read `TVPage.jsx`'s season/episode browsing fully before touching anything, per the plan's explicit instruction not to guess. Found the actual episode-card functionality is already solid (thumbnails, progress, watched state, download badges, restricted/unreleased gating, right-click context menu) — the real gap was purely visual: episode cards still used the pre-redesign 8px radius with no shadow, looking disconnected from every other card in the app, and the season selector would wrap into a cluttered multi-row block for long-running shows. Fixed both rather than guessing at a bigger rebuild the assessment didn't support.
- **Deviations:** None from plan §8/exec Phase 7d.
- **Known issues / next steps:** Not visually verified by the agent. This closes out Phase 7 (Layout & IA overhaul) — all four sub-phases (sidebar, hero cluster + Bento-by-default rows, genre browsing, episode-page UX) are done. Recommend the user do a full visual pass across Home, a TV show's episode list, and the sidebar before considering this redesign complete.

### 2026-06-18 — UI Redesign V3 Phase 7c: genre/category browsing
- **Changed:** New `src/components/GenreBrowser.jsx`, wired into `HomePage.jsx` directly below the hero cluster. Fetches TMDB's movie+TV genre lists once (merging genres that share a name, e.g. "Action" exists for both), renders them as pill chips reusing the existing `.genre-tag` style already used on the movie/TV detail pages. Clicking a chip fetches `/discover/movie` and `/discover/tv` filtered by that genre, sorted by popularity, merges the two lists, and renders the top 24 in the same Bento `cards-grid`/`MediaCard` system as every other row (first result gets the `featured` 2-span treatment). Clicking the active chip again clears the results. `global.css` — added `.genre-chip-row` (flex-wrap layout) and `.genre-tag--active`.
- **Decided:** This was entirely new feature work (no prior genre-browsing existed anywhere in the app, though the API caching layer in `utils/api.js` already had a long-TTL special case for `/genre/` paths that had never actually been used — a sign this was anticipated but never built). Reused the existing `.genre-tag` chip styling rather than inventing a new visual language for chips, and reused the Bento `cards-grid`/`MediaCard` system rather than building bespoke result cards, to keep this consistent with everything else on the page rather than a one-off bolt-on.
- **Deviations:** None from plan §8/exec Phase 7c.
- **Known issues / next steps:** Not visually verified by the agent. Genre results are not cached beyond TMDB's own response cache (re-selecting a genre after navigating away re-fetches) — acceptable for a v1, can add a session cache later if it feels slow in practice. Next: 7d (episode-page UX pass on `TVPage.jsx`).

### 2026-06-18 — UI Redesign V3 Phase 7b: hero cluster + Bento rows by default
- **Changed:** New `src/components/HeroQuickPicks.jsx` — small Bento tiles (backdrop crop + title/rating) shown beside the main hero banner, sourced from `topRatedItems` (distinct from the hero's own trending pool, so no duplicate content between the two). `src/pages/HomePage.jsx` — wraps `HeroBanner` + `HeroQuickPicks` in a new `.hero-cluster` grid (big featured tile ~69% width, quick-pick column ~31%, stacks vertically under 1000px). `global.css` — added `.hero-cluster`/`.hero-quickpicks*`/`.hero-quickpick-tile*` rules (Bento radius/shadow tokens, hover lift, reduced-motion override). `src/utils/homeLayout.js` — `loadHomeViewMode()`'s fallback default flipped from `"carousel"` to `"list"`, so the Bento `cards-grid`/`MediaCard`/`featured` system (built in Phase 3, previously only reachable via a Settings toggle) is what every row renders by default; `TrendingCarousel`'s coverflow is now the non-default option. `SettingsPage.jsx`'s "Row display style" toggle reordered (Bento Grid first) and relabeled to reflect the new default.
- **Decided:** Kept `HeroBanner`'s internals (rotation, trailer autoplay, mute toggle, save/library button) completely untouched — wrapping it rather than rewriting it avoided regression risk on its more complex logic, while still delivering the "multi-tile cluster" the plan called for via the new lightweight `HeroQuickPicks` tiles alongside it. Flipping the view-mode default (one-line change, mirrors how the accent-color default was flipped in the earlier Phase 6 bugfix) also fixes a layout inconsistency the user never explicitly complained about but that was contributing to the "doesn't feel designed" impression: previously "Continue Watching" always rendered as a grid while every other row rendered as a 3D coverflow — a jarring mixed layout. Now all rows render consistently as Bento grids by default.
- **Deviations:** None from plan §8/exec Phase 7b.
- **Known issues / next steps:** Not visually verified by the agent. Next: 7c (genre/category browsing) and 7d (episode-page UX pass on `TVPage.jsx`).

### 2026-06-18 — UI Redesign V3 Phase 7a: sidebar redesign (real layout change, not a recolor)
- **Changed:** Rewrote `src/components/Sidebar.jsx` and the `.sidebar*`/`.nav-btn*`/`.saved-row*` rules in `global.css`. Sidebar widens from a 72px icon-only rail to a 240px (`--sidebar` token) labeled nav panel: every nav item now shows an always-visible text label (not tooltip-only), reordered to Home → Search → Library & History → Downloads → One Pace (previously Search sat above Home with no clear rationale). Added a wordmark next to the logo. The "saved/library" thumbnail strip is now a "Pinned" section with a proper header, rendered as full-width rows (small thumb + title text) instead of a cramped column of bare square thumbnails — drag-to-reorder and right-click-to-remove both preserved. Removed the now-unused hover-tooltip mechanism (labels are always visible, so tooltips for nav items are redundant) and its leftover CSS (`.sidebar-btn .tooltip`, `.saved-thumb-tooltip`, stale `.saved-thumb.drag-over` outline rule superseded by a new inset-bar drag indicator on `.saved-row`).
- **Decided:** This is the first sub-phase of §8 (Layout & IA overhaul) — user feedback after Phases 0–6 was that the redesign "feels like you just changed the color," correctly identifying that no actual layout/IA work had happened yet. Did the sidebar first since it had the most concretely-described problems (no labels, arbitrary nav order, bolted-on pinned strip) and is fastest to validate before committing to the bigger hero/rows rework. `--sidebar` is consumed via CSS var by `.main`'s margin, a toast's position, and an episode-check notification pill — widening it required no changes to those three rules, just the token value.
- **Deviations:** None from plan §8/exec Phase 7a.
- **Known issues / next steps:** Not visually verified by the agent (sandbox can't launch the GUI) — user should run `npm run start` and confirm before continuing to 7b (hero cluster + Bento rows by default).

### 2026-06-18 — UI Redesign V3 Phase 6: reduced-motion sync + final pass
- **Changed:** `src/App.jsx` — the boot-time "Reduce Animations" check now ORs in `window.matchMedia("(prefers-reduced-motion: reduce)").matches` (plus a live `change` listener), so the app's blanket `body.no-anim` animation-killer activates automatically from the OS accessibility setting, not just the manual Settings toggle. `src/pages/SettingsPage.jsx` — `handleSave` in the Appearance section now also checks the live OS media query before toggling `no-anim`, so saving any other appearance setting (font size, compact mode) can no longer silently clobber an OS-driven reduced-motion state with the local checkbox's value. Ran `npm run dist:win` to verify the installer-icon acceptance criterion.
- **Decided:** Audited every transition/`@keyframes` added across Phases 3–5 — all had explicit reduced-motion handling already (per-feature `@media` overrides or JS `matchMedia` checks). Found a deeper, pre-existing architectural gap while doing that audit: `body.no-anim` (the app's blanket animation-killer, used everywhere) had only ever been wired to a manual Settings checkbox, never to the OS-level `prefers-reduced-motion` signal the plan's own acceptance criterion calls for — fixed it at the source so the fix benefits every transition/animation in the app, not just this redesign's new ones, rather than re-deriving the same gating logic per-feature.
- **Deviations:** None beyond what's already logged in today's other two entries (the appearance-picker fix, and this one).
- **Known issues / next steps:** Two acceptance criteria from `PLAN_UI_REDESIGN_V3.md` §5 structurally require a human and were not completed by the agent: (1) the full manual smoke test (play one movie + one TV episode + one anime episode + one One Pace episode) — this sandbox cannot launch Electron's real GUI; (2) final confirmation of the NSIS installer icon — `npm run dist:win` produced `dist/win-unpacked/MovieVault.exe` successfully (icon embedded), but the final installer-wrapper step failed on a Windows symlink-privilege error unrelated to this redesign (pre-existing sandbox limitation: `winCodeSign` extraction needs Developer Mode or an elevated shell). User should re-run `npm run dist:win` themselves with elevated privileges/Developer Mode for the final signed installer. All other acceptance criteria in §5 are met. This closes out the planned phases (0–6) of `PLAN_UI_REDESIGN_V3.md`; Tier 2 signature details (§7, items #5–13) remain optional/unstarted for a future session.

### 2026-06-18 — Fix: Phase 2's rename broke the pre-existing Accent Colour picker
- **Changed:** `src/utils/appearance.js` — `applyAccentColor()` now writes `--accent`/`--accent2`/`--accent-dim`/`--accent-glow` instead of the now-nonexistent `--red`/`--red2`/`--red-dim`/`--red-glow`. Added a new `"amber"` preset (`#ff8a3d`/`#ffaa66`, matching the locked brand palette) as the first entry in `ACCENT_PRESETS`. Recolored the `"red"` preset from the literal Netflix hex (`#e50914`/`#ff1a24`) to a generic red (`#dc2626`/`#ef4444`) — it's a legitimate user-choosable option, just no longer the exact eliminated brand color. `src/App.jsx` and `src/pages/SettingsPage.jsx` — default fallback when no preference is stored changed from `"red"` to `"amber"` (`storage.get(...) || "amber"`).
- **Decided:** While investigating the app's existing reduced-motion mechanism (`body.no-anim`, ahead of Phase 6), found `src/utils/appearance.js` — a fully-built, user-facing "Accent Colour" picker in Settings → Appearance (6 swatches: Red/Blue/Purple/Green/Orange/Pink) that nobody had surfaced earlier because every prior color audit in this initiative was scoped to `.jsx`/`.css` files and missed plain `.js` utility files. It called `document.documentElement.style.setProperty("--red", ...)` etc. on every app boot — since Phase 2 renamed every CSS consumer of `--red*` to `--accent*`, this picker had been a complete silent no-op since Phase 2 shipped (picking any color did nothing visible), and worse, its default "red" preset was the exact literal Netflix hex this whole redesign was meant to eliminate, reintroduced via inline style on every boot regardless of the `:root` token rewrite. Only changed the *fallback* default (used when nothing is stored) to amber, not any user's already-saved explicit choice — so someone who previously picked "Blue" keeps seeing blue; only fresh installs / users who never touched this setting get amber instead of red.
- **Deviations:** None — straightforward regression fix, not part of the original phase plan since the picker wasn't known to exist until now.
- **Known issues / next steps:** This is a good reminder that future redesign work on this repo should grep across all `.js` files too, not just `.jsx`/`.css`, when hunting for color literals. No other `.js` files had stale `--red*` references (verified via a full `src/` sweep). Continuing to Phase 6.

### 2026-06-18 — UI Redesign V3 Phase 5: Tier 1 signature details ("spells")
- **Changed:** `src/components/MediaCard.jsx` + `global.css` — added poster-tilt parallax (`onMouseMove` computes `rotateX`/`rotateY` from cursor position, applied via direct ref-mutated `transform`, no re-render, skipped under reduced-motion) and ambient color bleed (`sampleDominantColor()` averages an 8×12 offscreen-canvas downsample of each poster at TMDB's `w92` size, cached by URL in a module-level `Map`, written to a `--card-glow-color` CSS var consumed by `.card:hover`'s box-shadow). `src/components/OnePacePlayer.jsx` + `onepacePlayer.css` — bias-lighting: `requestVideoFrameCallback` samples the current frame into a 16×9 canvas every ~700ms, average color (scaled to 40% brightness) drives `--bias-bg` on `.onepace-player-container`, visible in the letterboxing around the `object-fit: contain` video. `src/pages/OnePacePage.jsx` + `global.css` — added a "Your Voyage" SVG map: one dot per arc along a sine-wave dashed path, filled/dim/empty per that arc's watch percent (reuses the existing `arcStats` memo), clickable/keyboard-focusable, calling the same `onSelectArc` as the arc cards.
- **Decided:** No new dependency needed for any of the four Tier 1 spells — all hand-rolled with Canvas/`requestVideoFrameCallback`/SVG per the plan's default policy. Two scope corrections made mid-implementation: (1) bias-lighting only applies to the One Pace player — Movie/TV/anime playback goes through Electron `<webview>` tags (cross-process, no pixel access), so there's no way to sample those frames; (2) the treasure-map is a voyage-wide overview across all arcs on `OnePacePage.jsx`, not a per-episode redo of the single-arc completion bar on `OnePaceArcPage.jsx` — re-reading the plan's own "filled dots per completed arc" wording, the more valuable and more literal reading is one dot per arc, not per episode. All four respect `prefers-reduced-motion` (tilt/hover-scale/bias-tint/dot-hover-scale all collapse to static; ambient glow and dot fill state are not gated since they're not motion).
- **Deviations:** `EXEC_UI_REDESIGN_V3.md` Phase 5 rewritten to reflect the two scope corrections above.
- **Known issues / next steps:** `npx vite build` succeeds after each spell; manual 60fps/visual verification in the running app is still outstanding (this sandbox can't launch the real Electron GUI). Tier 2 spells (#5–13) not started — optional/opportunistic per the plan. Next is Phase 6 (reduced-motion audit + full manual smoke test).

### 2026-06-18 — UI Redesign V3 Phase 4: page-by-page rollout (audit pass)
- **Changed:** `src/components/HeroBanner.jsx` — rating star color `#f5c518` → `var(--gold)` (matches the shared rating-display token); "More Info" button `borderRadius: 6` → `var(--radius)` (matches every other button). `src/pages/SettingsPage.jsx` — fixed a stale `var(--bg, #141414)` fallback to `var(--bg, #0a0a0a)` (didn't match the real `--bg` value, dead code in practice since `--bg` is always defined, but corrected for consistency).
- **Decided:** Phase 4 was originally scoped as a from-scratch re-skin of every page, but Phase 2's recolor sweep and Phase 3's `MediaCard`/`.cards-grid` work already touched all 23 component/page files and every grid-card surface. So this phase became an audit (confirm consistency, fix what's left) rather than a rebuild — see `EXEC_UI_REDESIGN_V3.md` Phase 4 for the full per-page breakdown. Confirmed `Skeleton.jsx`/`skeletons/*` need zero changes: every skeleton file reuses the real `.card`/`.detail-poster`/`.episode-card` CSS classes instead of hardcoding its own shapes, so they already inherited the Phase 3 Bento tokens automatically. Left several intentionally-distinct hardcoded colors alone (per-saga gradient theming in One Pace pages, subtitle-availability teal, generic success-green, provider-tag colors, "no match"/offline warning reds) since none of them are the literal Netflix red this redesign targets — chasing them would be unscoped color-unification work, not part of the locked plan.
- **Deviations:** Execution doc's Phase 4 description rewritten to reflect "audit pass" reality instead of the original "re-skin every page" framing.
- **Known issues / next steps:** `npx vite build` succeeds; a human visual/interaction pass across all pages is still the outstanding verification step (per `CLAUDE.md`/plan §5's full smoke test acceptance criterion). Next is Phase 5 (signature details / "spells") per `PLAN_UI_REDESIGN_V3.md` §7, then Phase 6 (reduced motion + final pass).

### 2026-06-18 — UI Redesign V3 Phase 3: Bento card component (`MediaCard`)
- **Changed:** `src/styles/global.css` — `.card` now uses `border-radius: var(--card-radius)` and `box-shadow: var(--card-shadow)`; the later Phase-4.3 `.card:hover` "polish" override (which wins on cascade order) now uses `var(--card-shadow-hover)` instead of its old hardcoded shadow; `.cards-grid` gap now reads `var(--card-gap)`. Added `.card--featured` (`grid-column: span 2`) and `.card--featured .card-poster` (`aspect-ratio: 16/9`). `src/components/MediaCard.jsx` — added a `featured` boolean prop that toggles the `card--featured` class; no other DOM/behavior changes. `src/pages/HomePage.jsx` — `renderList`'s `items.map` now passes `featured={idx === 0}` so the first card in each discovery row gets the featured span.
- **Decided:** Discovered that Home's default browsing view renders `TrendingCarousel` (an existing 3D coverflow widget with its own card markup), not `MediaCard`/`.cards-grid` — `renderList` only runs in "list" view mode. Per the Phase 0 sizing rule, the Bento featured-span treatment is scoped to `.cards-grid` usage (Continue Watching stays uniform, discovery rows get one featured tile) and `TrendingCarousel` is left as-is — reworking it into a Bento grid was never part of the plan and would be a much bigger, separately-scoped change.
- **Deviations:** None beyond the discovery above — execution doc updated to record it.
- **Known issues / next steps:** Code-level change only, verified via `vite build`; a human visual/interaction pass (hover popout, mark watched, context menu, featured-tile crop) in the running app is still needed. Next is Phase 4 (page-by-page rollout, starting with `Sidebar.jsx`).

### 2026-06-18 — UI Redesign V3 Phase 2: recolor off Netflix red (brand amber vs. danger red split)
- **Changed:** `src/styles/global.css` `:root` — replaced `--red`/`--red2`/`--red-dim`/`--red-glow` with `--accent`/`--accent2`/`--accent-dim`/`--accent-glow` (`#ff8a3d` amber), added `--violet: #7c6fe0`, added a new `--danger`/`--danger2`/`--danger-dim`/`--danger-glow` set (`#e5484d` — a red, but not the literal Netflix value), and added Bento tokens `--card-radius`/`--card-shadow`/`--card-shadow-hover`/`--card-gap`. Then recolored every one of the ~150 call sites that referenced the old tokens or literal `#e50914`/`rgba(229,9,20,...)`, across `global.css`, `onepacePlayer.css`, and 21 component/page files (`App.jsx`, `AsyncBoundary`, `DownloadModal`, `ErrorBoundary`, `Icons`, `MediaCard`, `OnePacePlayer`, `SearchModal`, `SetupScreen`, `Sidebar`, `SubtitleDownloaderModal`, `TrendingCarousel`, `UpdateModal`, `WatchPartyHostModal`, `WatchPartyIndicator`, `WindowTitlebar`, `DownloadsPage`, `HomePage`, `LibraryPage`, `MoviePage`, `OnePaceArcPage`, `OnePacePage`, `SettingsPage`, `TVPage`).
- **Decided:** Mid-sweep, found the old red was doing double duty as both the brand/CTA accent (Play buttons, active tabs, hover glows) and the danger/destructive/error color (delete buttons, factory reset, error boundaries, restricted-rating badges, "Kick guest", window close-button hover). Flagged this to the user before bulk-editing rather than guessing, since collapsing both onto one new accent would have made destructive actions visually indistinguishable from the primary CTA. User chose to keep a separate `--danger` red (not literally `#e50914`) for the destructive/error half. Every occurrence was then individually classified by its selector/prop context (not a blind find-and-replace) — e.g. `.age-rating-badge--restricted` → danger, `.btn-primary`/`.card-play` → accent, `.popout-rating` → repointed to the existing (previously unused) `--gold` token since it's a star-rating display, not a CTA or a warning. A few unrelated stray reds were normalized too: `WindowTitlebar`'s close-button hover and `SetupScreen`'s validation-error border were already using slightly different literal reds (`#e5091466`, `#f44336`) — both now route through `--danger` for consistency. Implementation used small targeted Node scripts (line-number-keyed find/replace) rather than dozens of manual edits, since many lines like `color: var(--red);` are textually identical but needed different treatment depending on surrounding context — a blind global replace would have been unsafe.
- **Deviations:** This phase was scoped in the execution plan as "replace `:root` tokens, keep names where roles match" — it turned out the literal Netflix red was referenced far more pervasively (~150 sites across 23 files, not just `global.css`) and needed semantic classification rather than a mechanical rename. `EXEC_UI_REDESIGN_V3.md` Phase 2 updated to reflect what actually happened.
- **Known issues / next steps:** Visual QA of the new amber/danger split across the app is still needed (this was a code-level sweep verified via `grep` + `vite build`, not a full visual pass per page). Next is Phase 3 (Bento card component / `MediaCard.jsx`).

### 2026-06-17 — UI Redesign V3 Phase 1: new app icon & brand mark
- **Changed:** Designed `public/brand/icon-master.svg` (hand-authored, matches `Icons.jsx`'s stroke-based style) — a dark rounded-square badge containing a film-reel/vault-dial mark (metallic disc, 6 punched holes, amber center hub) after an initial film-notch-and-play-triangle concept was rejected in favor of this reel/vault motif. Added `sharp` devDependency and `scripts/build-icons.mjs`, which rasterizes the master to `public/brand/icon-1024.png` (electron-builder source) and `public/brand/icon-256.png` (runtime window icon); wired both into `package.json` (`build.win.icon`, `build.mac.icon`, `build.linux.icon`, new `build:icons` script) and `index.js` (`BrowserWindow` icon — now applied on all platforms, not just Linux as before). Deleted `public/icon.png`, all 7 files in `public/sized/`, and `public/logo.svg` via `git rm` (recoverable from history). Renamed `StreambertLogo` → `AppLogo` in `src/components/Icons.jsx`, pointed at the new SVG; updated both call sites — `src/components/Sidebar.jsx` and `src/components/SetupScreen.jsx` (the second one wasn't listed in the execution plan's file list, found during the actual rename). Fixed the literal-red drop-shadow on `.sidebar-logo img` in `global.css` to the new amber accent. Fixed a stale "Streambert main process" comment in `index.js`.
- **Decided:** First icon concept (film-strip notches + amber play triangle) was built, previewed at 16–256px, and rejected by the user in favor of a film-reel/vault-dial motif — better ties "Vault" into the mark and reads as a more distinct disc-shaped badge. Used `git rm` instead of a raw `rm -rf` for the retired assets so the deletion is reversible via git history (a sandbox permission gate blocked the raw `rm -rf` outright). Left scattered "Streambert" *text* strings (User-Agent headers, Settings page copy) untouched — out of scope for this redesign, which only covers the icon/logo component per `PLAN_UI_REDESIGN_V3.md` §2.
- **Deviations:** `EXEC_UI_REDESIGN_V3.md`'s Phase 1 file list only mentioned `Sidebar.jsx` as a `StreambertLogo` call site; `SetupScreen.jsx` was a second one, found and fixed during execution (would have been a broken import otherwise). Execution-doc Phase 1 updated to note this.
- **Known issues / next steps:** This sandbox runs Electron with `ELECTRON_RUN_AS_NODE=1`, so the agent could not launch the real GUI to visually confirm the taskbar/window icon — `npx vite build` succeeded and `dist/brand/` contains the right files, but the user needs to run `npm run start` themselves to confirm the icon renders correctly, and `npm run dist:win` (or another platform) to confirm the installer icon. Next is Phase 2 (design tokens in `global.css`) once the icon is visually confirmed.

### 2026-06-17 — UI Redesign V3 Phase 0: lock palette, Bento sizing rule, icon production method
- **Changed:** Created `redesignuiux` branch off `main` to isolate this work. Created `refdocs/plans/v3-palette-swatch.html` (throwaway static HTML gut-check, not shipped). Updated `refdocs/plans/PLAN_UI_REDESIGN_V3.md` §4 (Color palette, Layout pattern, App icon rows) and §6 (questions 1–3 marked resolved with decisions inline). Updated `refdocs/execution/EXEC_UI_REDESIGN_V3.md` Phase 0 (marked done) and Phase 1 (files to create/modify firmed up: `public/brand/icon-master.svg`, `scripts/build-icons.mjs`, `sharp` devDependency, no more hand-maintained `public/sized/*` ladder).
- **Decided:** Palette — surfaces stay near-black, unchanged from today's `global.css` (`--bg #0a0a0a`, `--surface #111111`, `--surface2 #1a1a1a`, `--surface3 #222222`, `--border #2a2a2a`); only the accent changes, `--red`/`--red2`/`--red-dim`/`--red-glow` → `--accent #FF8A3D` / `--accent2 #FFAA66` / `--accent-dim` / `--accent-glow` (warm amber); added `--violet #7C6FE0` as a sparing secondary accent; kept the existing unused `--gold #c8a84b` reserved for star ratings only so it doesn't collide visually with the new amber CTA. User's explicit call after viewing the swatch: the literal "Netflix-ness" was the red accent, not the near-black mood, so the surfaces didn't need re-tinting indigo/violet as first proposed. Bento sizing — first item in discovery rows (Trending/Popular/Recommended/genre) gets a 2x1 featured span, everything else 1x1; utility rows (Continue Watching, Library, Downloads, search, episode grids) stay uniform 1x1 for scannability. Icon production — hand-author the master as an SVG (not image-gen, to guarantee it matches `Icons.jsx`'s existing stroke-based path language) and add `sharp` as a devDependency to rasterize it; electron-builder auto-generates per-platform `.ico`/`.icns`/Linux icons from one 1024×1024 master PNG, removing the need to hand-maintain the 7-file `public/sized/*` ladder.
- **Deviations:** First swatch draft proposed re-tinting the surfaces toward deep indigo/violet; user asked for a darker, closer-to-current-app direction instead, so the surfaces were reverted to today's exact near-black values and only the accent/secondary tokens changed.
- **Known issues / next steps:** Phase 0 is fully resolved (open questions 1–3 closed; #4 web-app follow-up remains intentionally deferred). Next is Phase 1 (new app icon & brand mark) per `EXEC_UI_REDESIGN_V3.md`.

### 2026-06-17 — Add "Signature details" (design spells) to UI Redesign V3 plan
- **Changed:** `refdocs/plans/PLAN_UI_REDESIGN_V3.md` — added §7 "Signature details (\"spells\")" listing 13 micro-interaction/personality ideas from a `design-spells` skill pass, split into Tier 1 (build first: poster-tilt parallax, ambient color bleed, player bias-lighting, One Pace treasure-map arc progress) and Tier 2 (stretch: spine-stack progress, hero idle parallax, projector-flicker title reveal, scrubber thumbnail trail, straw-hat sidebar indicator, Watch Party countdown ritual, Watch Party reaction physics, deadpan `BlockedStatsModal` copy, film-reel loader). Softened §3 Constraints and the §4 animation row from a blanket "no new dependency" to "vanilla CSS/Canvas/JS by default, small dependency allowed if a specific spell genuinely needs one." Added a Tier 1 acceptance criterion to §5. Fixed two stale `§9` cross-references (now `§6`). `refdocs/execution/EXEC_UI_REDESIGN_V3.md` — inserted a new Phase 5 "Signature details (\"spells\") pass" (Tier 1 implementation, file targets, per-spell 60fps/layout-shift/reduced-motion checks) ahead of the renumbered Phase 6 (was Phase 5) "Reduced motion & final pass," and added a rollback note for the new phase.
- **Decided:** User approved all 13 spells for the plan and explicitly relaxed the no-new-dependency constraint: "if a new feature is needed for the design to function then ill gladly have them." Kept the policy narrow (dependency must be justified by a specific spell, not open-ended) rather than removing the constraint outright, since the user's wording was permissive-on-justification, not permissive-by-default.
- **Deviations:** None — this is a planning-doc-only update, no component code touched yet.
- **Known issues / next steps:** Implementation still hasn't started — Phase 0 (lock palette hex values, icon production method, Bento sizing rule) remains the next concrete step before any component code is written.

### 2026-06-17 — Fix MoviePage crash: blockedSession/blockedAlltime/getBlockedDomains undefined
- **Changed:** `apps/web/src/pages/MoviePage.jsx` — expanded the `useBlockedStats(item.id)` destructure to also map `sessionTotal: blockedSession`, `alltimeTotal: blockedAlltime`, and `getSessionDomains: getBlockedDomains`.
- **Decided:** The earlier `showBlockedModal` fix only pulled `showModal`/`setShowModal` from the hook, but MoviePage also references `blockedSession` (player ad-block badge), `blockedAlltime`, and `getBlockedDomains()` (BlockedStatsModal props) — so the page crashed with `blockedSession is not defined`. Now mirrors TVPage's complete destructure of all five hook returns.
- **Deviations:** None — completes the incomplete prior fix.
- **Known issues / next steps:** None for this crash.

### 2026-06-17 — Plan full UI redesign (V3) for the Electron app
- **Changed:** No code changes. Created `refdocs/plans/PLAN_UI_REDESIGN_V3.md` and `refdocs/execution/EXEC_UI_REDESIGN_V3.md`; updated `refdocs/plans/README.md` and `refdocs/execution/README.md` index tables.
- **Decided:** Scoped a full visual redesign of `src/` (Electron app only — `apps/web/src/` deferred). Direction locked via user Q&A: new color palette (moving away from the literal Netflix red/black currently in `global.css`), Bento-style modular card grid (replacing the dense uniform grid), Netflix's hero-banner/row-density kept as structural inspiration only, typography (Bebas Neue + DM Sans) unchanged. New app icon will be a fresh mark drawn in the existing `Icons.jsx` line-art SVG style, replacing `public/icon.png`, `public/sized/*`, and the leftover "Streambert"-branded `public/logo.svg`/`StreambertLogo` component. `IMPROVEMENT_PLAN_V2.md` is fully shipped (P4+P5+P6) and is not superseded — V3 is a standalone follow-on plan.
- **Deviations:** `IMPROVEMENT_PLAN_V2.md` §4.3 assumed `framer-motion` was already a dependency; confirmed during planning that it is **not** in `package.json` and is unused in `src/` — the codebase uses hand-written CSS transitions only. V3's animation approach (plain CSS, no new dependency) was set accordingly; corrected the execution-plan status table (V2 was previously marked "P6 undeployed" — user confirmed it's all shipped).
- **Known issues / next steps:** Plan has 4 open questions (exact palette hex values, icon production method, Bento sizing rule, web-app follow-up timing) to resolve in Execution Phase 0 before any component code is touched. No implementation has started yet.

### 2026-06-17 — Fix serverless functions crashing (ESM vs CommonJS) — proxy + AllManga
- **Changed:** Converted `apps/web/api/proxy.js` and `apps/web/api/resolve-allmanga.js` from CommonJS (`require`/`module.exports`) to ESM (`import`/`export default`).
- **Decided:** Verified against the live deployment that BOTH serverless functions returned 500 `FUNCTION_INVOCATION_FAILED` with runtime log `ReferenceError: require is not defined`. Cause: `apps/web/package.json` has `"type": "module"`, so Vercel treats `.js` function files as ESM, where `require`/`module.exports` don't exist. **First attempt** renamed them to `.cjs` — but that made Vercel's Vite preset stop detecting them as functions entirely (deploy showed no `lambdaRuntimeStats`; `/api/proxy` returned 404). Vercel only auto-detects `.js`/`.ts`/`.mjs` in `api/` for this project, so the correct fix is to keep `.js` and convert the source to ESM. Fixes both the One Pace Pixeldrain proxy and AllManga (anime) resolution — AllManga playback on web was broken by the same root cause.
- **Deviations:** Reverted the `.cjs` rename after confirming via deploy metadata that those files weren't being built as functions.
- **Known issues / next steps:** Re-verify on deploy: `/api/proxy` should return 206 for the One Pace Pixeldrain URL, unblocking One Pace playback end-to-end.

### 2026-06-17 — Fix One Pace streaming via serverless proxy (Pixeldrain embed block)
- **Changed:** `apps/web/src/utils/onepaceApi.js` — route Pixeldrain stream URLs through the existing `/api/proxy` serverless function in production; bumped cache key `v2` → `v3`. Also removed `crossOrigin="anonymous"` from the One Pace `<video>` and `sandbox` from the Movie/TV player iframes (prior commit) — those were necessary but not sufficient.
- **Decided:** Diagnosed with Playwright against the live site: browser fetches of `pixeldrain.com/api/file/{id}` return **403 `{"value":"embed_not_allowed"}`**, while identical server-side requests (curl / the proxy function) return **206 video/mp4**. Pixeldrain blocks browser-originated embeds (Sec-Fetch / browser signal), not by Origin/Referer. The same-origin `/api/proxy` streams the file server-side with Range passthrough, bypassing the block. Gated on `import.meta.env.PROD` so local `vite dev` (no serverless functions) keeps hitting Pixeldrain directly, which already works there.
- **Deviations:** Reused the existing generic `api/proxy.js` rather than writing a Pixeldrain-specific endpoint — it already forwards Range/Content-Range and sets CORS.
- **Known issues / next steps:** Proxying ~400 MB files through a Vercel function uses bandwidth/compute; acceptable for now given Range requests pull only what's played. Subtitle fetches from `onepace.arl.sh` may still hit CORS (caught/skipped, non-fatal). Movie/TV iframe sources: Videasy (default) frames fine on the live site; vidsrc.to / 2embed (marked "unstable") time out — those are flaky third parties, not an app bug.

### 2026-06-17 — Fix two web app crashes (showBlockedModal + One Pace streaming)
- **Changed:** `apps/web/src/pages/MoviePage.jsx` — added missing `useBlockedStats(item.id)` call to declare `showBlockedModal`/`setShowBlockedModal` (used in JSX but never declared, causing a hard crash on the movie page). `apps/web/src/utils/onepaceApi.js` — removed `?download` from Pixeldrain stream URL (download-mode response cannot be streamed by the browser's `<video>` element); bumped cache key from `v1` → `v2` to invalidate stale cached URLs.
- **Decided:** Used `item.id` as the `useBlockedStats` reset key in MoviePage (consistent with per-media reset semantics). Cache key bump is the cleanest way to force a re-fetch without requiring users to clear localStorage.
- **Deviations:** None from intent; these were both missing from the One Pace port.
- **Known issues / next steps:** Pixeldrain streams may still hit CORS on some browsers. If that surfaces, the "Copy Pixeldrain Link" fallback remains available.

### 2026-06-17 — Port One Pace feature to web app
- **Changed:** Created `apps/web/src/pages/OnePacePage.jsx`, `OnePaceArcPage.jsx`, `apps/web/src/components/OnePacePlayer.jsx`, `apps/web/src/utils/onepaceApi.js`, `apps/web/src/utils/onepaceMapping.js`, `apps/web/src/styles/onepacePlayer.css`. Added `PauseIcon`, `MaximizeIcon`, `VolumeIcon`, `VolumeMuteIcon`, `StrawHatIcon` to `apps/web/src/components/Icons.jsx`. Added One Pace nav button (StrawHat) to web Sidebar. Added `onepace`/`onepaceArc`/`onepacePlayer` routes and lazy imports to web `App.jsx`.
- **Decided:** Pages self-fetch the catalog rather than receiving it from App via props. Electron IPC caching replaced by `localStorage` cache (6h TTL) in `onepaceApi.js`, fetching the two public GitHub raw URLs directly. `window.electron.openExternal` → `window.open(..., "_blank")`. AniList sync kept since web `utils/oauth.js` exists.
- **Deviations:** `OnePacePage` fetches its own arcs (self-contained) instead of receiving `arcs` as a prop — cleaner for web since there's no IPC layer. Watch party props removed from web player.
- **Known issues / next steps:** Pixeldrain direct-stream URLs may hit browser CORS; users can use "Copy Pixeldrain Link" fallback. Subtitle fetch from `onepace.arl.sh` may also need CORS headers — verify after deploy.

### 2026-06-17 — .claude operating layer scaffolded
- **Changed:** Created `.claude/` with `hooks/check_secrets.py` (blocks hardcoded TMDB keys/JWTs in source), `hooks/check_polyfill_sync.py` (warns when `preload.js` is edited without updating `polyfill.js`), `memory/preflight.md` (session-start checklist), and `settings.json` (permissions allowlist + hook wiring).
- **Decided:** Two hooks: one hard-blocking (secrets), one warning-only (polyfill sync). No format/lint hook — no ruff/pyright in this JS project.
- **Deviations:** None.
- **Known issues / next steps:** Hooks require Python 3 on PATH. On Windows, `python3` may need to be `python` — test with a dummy write if hooks don't fire.

### 2026-06-17 — Repo documentation reorganization
- **Changed:** Created `refdocs/` folder structure with subfolders `changelog/`, `plans/`, `execution/`, `guides/`. Moved all loose `.md` files from root into the appropriate subfolder.
- **Changed:** Created `CLAUDE.md` at project root to brief AI agents on the two-codebase structure (Electron `src/` vs Web `apps/web/src/`) and mandatory doc rules.
- **Decided:** All future improvement plans go in `refdocs/plans/`, all execution plans in `refdocs/execution/`, all AI session logs here in `refdocs/changelog/CHANGELOG.md`.
- **Deviations:** None — pure housekeeping, no code changed.
- **Known issues / next steps:** Web app (`apps/web/src/`) still being updated to match V2 Electron UI (HeroBanner, CastRow, RatingBadge, SimilarRow in progress).

---

### 2026-05-24 — Phase 4.3: Page Transitions & Card Hover
- **Changed:** Premium 180ms fade+slide page transitions on forward navigation (instant on back). Card hover scales to 1.08× with lift shadow; tactile 0.96× active-state click feedback; custom focus ring with red outline+glow. 600ms hover delay triggers an `InfoPopout` overlay via React Portal with title, rating, year, runtime, genres, and synopsis. Arrow-key grid navigation with geometric coordinate-based DOM search.
- **Decided:** All effects bypass when `prefers-reduced-motion: reduce`.
- **Deviations:** Used pure CSS keyframes instead of `framer-motion` (not installed); kept same UX outcome.
- **Known issues / next steps:** None.

### 2026-05-24 — Phase 4.2: Hero Banner with Trailer Autoplay
- **Changed:** Full-width `<HeroBanner>` component replaces the static `.hero` block on HomePage. Shows 5 rotating featured titles (3 trending movies + 2 trending TV, interleaved). Auto-advances every 12s when not hovered. After 2s of hover/visibility, fetches the TMDB `/videos` trailer endpoint and fades in a muted YouTube iframe. Unmute/mute toggle sends `postMessage` to YouTube iframe. "Play" CTA passes `playDirectly:true`. "Add to Library" syncs with global save state.
- **New files:** `src/components/HeroBanner.jsx`
- **Modified files:** `src/pages/HomePage.jsx`, `src/pages/MoviePage.jsx`, `src/pages/TVPage.jsx`, `src/utils/storage.js`, `src/pages/SettingsPage.jsx`, `src/styles/global.css`
- **Decided:** Settings → Playback "Autoplay trailers on banner" toggle hard-bypasses the trailer path.
- **Deviations:** Used pure CSS opacity transitions instead of `framer-motion` cross-fade; IntersectionObserver covers the "visibility" trigger from the spec.
- **Known issues / next steps:** YouTube `enablejsapi=1` postMessage mute/unmute requires Electron `webSecurity: false`.

### 2026-05-24 — Phase 4.2: Error State System
- **Changed:** Centralized error codes utility in `src/utils/errors.js`. Reusable `<AsyncBoundary>` component with loading, error, and empty states. Exponential backoff cooldown (1s, 2s, 5s) for retry button. Safe-wrapping IPC registration helper `safeHandle` in `index.js`. Offline warning banner at top of main layout.
- **Deviations:** None.
- **Known issues / next steps:** None.

### 2026-05-24 — Phase 4.1: Skeleton Loading System
- **Changed:** Shape-matched skeleton loading system across Home, Library, Movie Details, TV Details, and Search. Low-contrast shimmering skeletons aligned to final layout to eliminate layout shifts. Respects `prefers-reduced-motion`.
- **New dependencies:** None (native HTML/CSS only).
- **Deviations:** None.
- **Known issues / next steps:** None.
